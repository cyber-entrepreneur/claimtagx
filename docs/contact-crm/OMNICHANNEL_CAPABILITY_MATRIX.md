# Omnichannel capability matrix (frozen inbox scope)

Local fixture verification is independent of live provider verification. Live columns stay **blocked** until first-party credentials and an approved round trip exist. Simulators never count as LIVE_VERIFIED.

| Channel | Inbound | Outbound | Webhook verification | Polling/delta | Text | Attachments | Delivery/read receipts | Reply-window | Local fixture | Live provider | Honest status | Exact external requirement |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Website Contact | Implemented (form → conversation) | Implemented (email reply job) | N/A (no public webhook) | No | Yes | **false** — not offered on the form | **false** — not implemented | None | Yes | Blocked until durable inbound+outbound evidence without simulators | IMPLEMENTED_AWAITING_CREDENTIALS until `live_verified_at` | Public site + transactional email (not an env flag) |
| Microsoft 365 | Implemented (Graph notification + delta poller) | Implemented (Graph/email send) | Client-state + `validationToken` | Yes | Yes | **false** — download/quarantine not implemented | **false** — not mapped | None | Fixture/simulator | Blocked without Entra app + mailbox | IMPLEMENTED_AWAITING_CREDENTIALS | Entra tenant, app permissions, admin consent, Exchange mailbox, notification URL, client state |
| WhatsApp Cloud API | Implemented (Cloud API webhook) | Implemented (text + template send) | `X-Hub-Signature-256` over raw bytes | No | Yes | **false** — temporarily unavailable pending controlled download | Delivery **true**, read **true** (status webhooks) | 24h | Signature + normalize fixtures | Blocked without Meta credentials | IMPLEMENTED_AWAITING_CREDENTIALS or BLOCKED_APP_REVIEW | Meta business, phone number id, access token, app secret, verify token, app review if required |
| Messenger | Implemented | Implemented (Send API text) | Same Meta SHA-256 raw-body signature | No | Yes | **false** — temporarily unavailable | Delivery **true** (mids); read **false** | 24h | Signature + normalize fixtures | Blocked | IMPLEMENTED_AWAITING_CREDENTIALS or BLOCKED_APP_REVIEW | Meta app, Page token, `pages_messaging`, webhook |
| Instagram Messaging | Implemented | Implemented (Page send) | Same Meta SHA-256 raw-body signature | No | Yes | **false** — temporarily unavailable | Delivery **true**; read **false** | 24h | Signature + normalize fixtures | Blocked | IMPLEMENTED_AWAITING_CREDENTIALS or BLOCKED_APP_REVIEW | Professional account, connected Page, `instagram_manage_messages` |
| X Direct Messages | Implemented (Account Activity parse) | Implemented (X API v2 DM send) | GET CRC-HMAC; POST requires official `X-Twitter-Webhooks-Signature` or **fail closed** | No | Yes | **false** — temporarily unavailable | **false** — not mapped | None | CRC + signature fixtures | Blocked | IMPLEMENTED_AWAITING_CREDENTIALS | X developer account, consumer secret, access token, Account Activity webhook, paid API tier |
| TikTok | Manual handoff only | Disabled | Rejected | No | No | false | false | N/A | Reject send/webhook | Not applicable | UNSUPPORTED_BY_PUBLIC_API | Official public support DM API (not available; no scraping) |
| LinkedIn | Manual handoff only | Disabled | Rejected | No | No | false | false | N/A | Reject send/webhook | Not applicable | PARTNER_GATED | Approved LinkedIn partner private-messaging access |

Reconciliation is **false** on every channel until a real provider status lookup exists. Internal job/effect idempotency is durable and does not use unofficial provider idempotency headers.
